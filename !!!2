sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '1 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_1_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '2 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_2_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '3 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_3_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '4 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_4_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '5 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_5_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '6 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_6_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '7 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_7_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '8 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_8_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '9 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_9_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '10 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_10_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '11 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_11_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '12 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_12_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '13 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_13_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '14 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_14_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '15 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_15_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '16 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_16_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '17 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_17_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '18 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_18_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '19 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_19_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '20 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_20_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '21 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_21_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '22 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_22_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '23 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_23_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '24 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_24_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '25 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_25_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '26 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_26_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '27 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_27_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '28 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_28_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '29 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_29_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '30 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_30_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '31 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_31_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '32 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_32_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '33 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_33_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '34 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_34_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '35 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_35_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '36 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_36_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '37 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_37_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '38 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_38_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '39 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_39_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '40 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_40_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '41 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_41_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '42 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_42_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '43 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_43_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '44 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_44_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '45 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_45_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '46 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_46_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '47 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_47_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '48 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_48_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '49 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_49_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '50 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_50_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '51 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_51_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '52 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_52_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '53 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_53_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '54 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_54_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '55 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_55_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '56 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_56_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '57 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_57_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '58 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_58_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '59 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_59_MONTH_11_APPROVED,
sum(case when RA.REFUNDCATEGORY not in ('Выплата по исполнительному листу', 'Выплата до решения суда') and
             RA.TRNL_UPDATETIME < date_trunc('month', CP.LOSSDATE) + interval '60 month' + (p_indent * interval '1 day')
        then RA.FOR_APPROVEDSUM
        else 0
    end) PLUS_60_MONTH_11_APPROVED
